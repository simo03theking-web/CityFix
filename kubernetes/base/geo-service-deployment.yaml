apiVersion: v1
kind: Service
metadata:
  name: geo-service
  namespace: cityfix
spec:
  selector:
    app: geo-service
  ports:
  - protocol: TCP
    port: 8005
    targetPort: 8005
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: geo-service
  namespace: cityfix
spec:
  replicas: 2
  selector:
    matchLabels:
      app: geo-service
  template:
    metadata:
      labels:
        app: geo-service
    spec:
      containers:
      - name: geo-service
        image: cityfix/geo-service:latest
        ports:
        - containerPort: 8005
        env:
        - name: SERVICE_PORT
          value: "8005"
        - name: MONGODB_URL
          value: "mongodb://$(MONGO_USER):$(MONGO_PASSWORD)@mongodb:27017/cityfix?authSource=admin"
        - name: MONGO_USER
          valueFrom:
            secretKeyRef:
              name: mongodb-secret
              key: username
        - name: MONGO_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongodb-secret
              key: password
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 5
